#include <cstdio>
#include <algorithm>
using namespace std;

#define FOR(i,a,b) for(int i=(a);i<(b);i+=1)
#define REP(i,n)  FOR(i,0,n)

int n;
void solve();

int main() {
  for (;;) {
    // 入力
    scanf("%d", &n);
    if (n == 0) return 0;
    REP(i, n) {
      // scanf("%d", &a[i]);
    }

    solve();

    // 出力
    printf("%d\n", 0);
  }
}

void solve() {
}
